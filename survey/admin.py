survey_fields = ('survey', 'survey_schedule')

survey_fieldset_tuple = (
    'Survey', {
        'classes': ('collapse',),
        'fields': survey_fields})


survey_schedule_fields = ('survey_schedule', )

survey_schedule_fieldset_tuple = (
    'Survey', {
        'classes': ('collapse',),
        'fields': survey_schedule_fields})
